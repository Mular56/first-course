from array import * 
myArray =  [1, 2, 3], [-1, 0, 1]

for raw in myArray:
    for raw in myArray:
        if raw > 0:
            print(raw)

for raw in myArray:
    for raw in myArray:
        if raw == 0:
            print(raw)

print(myArray)